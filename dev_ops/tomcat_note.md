# tomcat note

## note

```text
    修改默认端口号
        /usr/local/tomcat/conf/server.xml



```

## command

```shell
    cd /usr/local/tomcat/bin
    ./shutdown.sh               # 关闭命令Tomcat
    ps -ef|grep java            # 查看Tomcat是否以关闭
    ./startup.sh                # 启动Tomcat


```
